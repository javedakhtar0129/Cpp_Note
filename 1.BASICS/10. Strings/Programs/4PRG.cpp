#include<iostream>
#include<string>
using namespace std;
void duplicate(string str);
int main(){
    string str; char X;
    cout<<"Enter your string: ";
    getline(cin,str);

    duplicate(str);

    return 0;
}

void duplicate(string str){
    for(int i=0;i<str.length();i++){
        
    }
}